{
  "name": "chess-analytics",
  "version": "1.0.0",
  "main": "electron/main.mjs",
  "scripts": {
    "electron:dev": "concurrently \"npm run dev --prefix frontend\" \"wait-on http://localhost:5173 && electron .\"",
    "electron:build": "npm run build --prefix frontend && npm run build --prefix backend && electron-builder",
    "frontend:build": "npm run build --prefix frontend",
    "backend:build": "npm run build --prefix backend"
  },
  "dependencies": {
    "better-sqlite3": "^12.6.2",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "chess.js": "^1.0.0-beta.8",
    "bull": "^4.12.2",
    "ioredis": "^5.3.2",
    "stockfish": "^16.0.0",
    "axios": "^1.6.7",
    "zod": "^3.22.4",
    "dotenv": "^16.4.1"
  },
  "devDependencies": {
    "@electron/rebuild": "^4.0.3",
    "@types/cors": "^2.8.19",
    "@types/express": "^4.17.25",
    "@types/node": "^20.19.30",
    "concurrently": "^9.2.1",
    "electron": "^40.0.0",
    "electron-builder": "^26.4.0",
    "esbuild": "^0.20.2",
    "wait-on": "^9.0.3"
  }
}